const track=document.querySelector('#reviews-track');const viewport=document.querySelector('.reviews__viewport');const dots=document.querySelector('#reviews-dots');const statusEl=document.querySelector('#reviews-status');const prev=document.querySelector('.reviews__arrow--prev');const next=document.querySelector('.reviews__arrow--next');let reviews=[],index=0,autoplay=null,startX=null,deltaX=0;const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function render(){track.innerHTML=reviews.map((r,i)=>{const name=esc(r.name||'Гость');const rating=Math.max(1,Math.min(5,Number(r.rating)||5));const avatar=r.avatar?`<img class="review-card__avatar" src="${esc(r.avatar)}" alt="" loading="lazy">`:`<div class="review-card__avatar review-card__avatar--placeholder">${name[0]||'Г'}</div>`;return `<article class="review-card" data-index="${i}" tabindex="0"> <div class="review-card__top">${avatar}<div><h3 class="review-card__name">${name}</h3><div class="review-card__stars" aria-label="Оценка ${rating} из 5">${'★'.repeat(rating)}${'☆'.repeat(5-rating)}</div></div></div><p class="review-card__text">${esc(r.text)}</p>${r.date?`<div class="review-card__date">${esc(r.date)}</div>`:''}</article>`}).join('');dots.innerHTML=reviews.map((_,i)=>`<button class="reviews__dot" data-index="${i}" aria-label="Отзыв ${i+1}"></button>`).join('');track.querySelectorAll('.review-card').forEach(el=>{el.onclick=()=>go(Number(el.dataset.index));el.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();go(Number(el.dataset.index))}}});dots.querySelectorAll('button').forEach(el=>el.onclick=()=>go(Number(el.dataset.index)));requestAnimationFrame(update)}function update(){const cards=[...track.children];if(!cards.length)return;index=(index+cards.length)%cards.length;const active=cards[index];const target=active.offsetLeft-(viewport.clientWidth-active.offsetWidth)/2;track.style.transform=`translate3d(${-target}px,0,0)`;cards.forEach((c,i)=>{c.classList.toggle('is-active',i===index);c.classList.toggle('is-near',Math.abs(i-index)===1)});dots.querySelectorAll('button').forEach((d,i)=>d.classList.toggle('is-active',i===index))}function go(i,restart=true){if(!reviews.length)return;index=(i+reviews.length)%reviews.length;update();if(restart)startAuto()}function startAuto(){clearInterval(autoplay);if(reviews.length>1&&!matchMedia('(prefers-reduced-motion:reduce)').matches)autoplay=setInterval(()=>go(index+1,false),6000)}prev.onclick=()=>go(index-1);next.onclick=()=>go(index+1);addEventListener('resize',update);viewport.addEventListener('pointerdown',e=>{startX=e.clientX;deltaX=0;clearInterval(autoplay)});viewport.addEventListener('pointermove',e=>{if(startX!==null)deltaX=e.clientX-startX});viewport.addEventListener('pointerup',()=>{Math.abs(deltaX)>45?go(index+(deltaX<0?1:-1)):startAuto();startX=null});const API_BASE=window.KENGURU_REVIEWS_API||((location.hostname==='kenguru-reviews.onrender.com'||location.hostname.endsWith('.onrender.com'))?'':'https://kenguru-reviews.onrender.com');async function load(force=false){statusEl.textContent='Загружаем реальные отзывы…';try{const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),90000);const response=await fetch(`${API_BASE}/api/reviews${force?'?refresh=1':''}`,{cache:'no-store',mode:'cors',signal:controller.signal});clearTimeout(timer);const data=await response.json();if(!response.ok||!data.reviews?.length)throw new Error(data.message||'Отзывы не получены');reviews=data.reviews;render();startAuto();statusEl.textContent=data.stale?'Показана сохранённая копия. Обновление выполняется автоматически.':''}catch(error){statusEl.textContent=`Отзывы временно недоступны. ${error.message||''}`}}load();setInterval(()=>load(true),60*60*1000);
